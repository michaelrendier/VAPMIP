__pycache__/
*.pyc
*.o
monad_wordnet.bin
*.bin.bak
PtolC/ptolemy
ptolemy-monad
monad_sedenion.bin
monad_sedenion_addresses.pkl
auth_totp_secret.h
tools/holcus_totp_enroll.png

# Field state — never committed
*.bin
*.ptol
*.pkl
*.sock

# Secrets — never committed
.env
.env.*
secrets/
*_secret*
*_token*
*_key*
*_credentials*

# Voice artefacts
*.vprint
*.vp
*.voiceprint
voiceprint.*

# Session state
*.session
session_state*
field_session*

# Spectral working files
*.spectral
spectral_cache/
resonance_cache/

# Ptolemy home mirrors — if ever symlinked into repo
live_state.json
ingest_state.json
*.ptolrc
journal.jsonl
states/

# OS
.DS_Store
Thumbs.db

# Compiled binaries — build artifacts, rebuilt from source, never committed.
PtolC/ptol
ahura-mazda
bumblebee
totp_test

# python venv (2026-08-15) — see env.sh
.venv/

# Local Claude Code overrides — machine state, not source
.claude/settings.local.json

# compiled harness binaries
PtolC/rotary_rerun
sizecheck

# the shell binary — build with: cc -O2 -std=c99 -o ptol ptol_shell.c -lm
PtolC/ptol
PtolC/ptol_asan

# External grammar corpora — provenance tracked via datasets/README.md + fetch.sh
datasets/*/

# grammar index — regenerable via: python3 -m engine.grammar.build
monad_sentences.json
monad_sentences.bin
